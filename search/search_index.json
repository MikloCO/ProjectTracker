{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Project Tracker","text":"<p>Project Tracker is a desktop application for tracking progress across multiple online courses. Add courses, tick off chapters as you complete them, take rich-text notes, and collect screenshots \u2014 all in one place.</p>"},{"location":"#features","title":"Features","text":"<ul> <li> <p> Course management</p> <p>Add courses with a title, provider, link, banner image, and category. Set a chapter count and track each one individually.</p> </li> <li> <p> Progress tracking</p> <p>Every course displays a live progress bar that updates the moment you tick a chapter as complete.</p> </li> <li> <p> Workbook</p> <p>Each course has its own rich-text workbook. Write notes with bold, italic, bullet lists, and numbered lists. Notes are auto-saved per course.</p> </li> <li> <p> Gallery</p> <p>Attach reference images or screenshots to any course. Images are stored locally and displayed in a scrollable gallery.</p> </li> <li> <p> Three views</p> <p>Switch between Overview (in-progress), Browse (to-do), and Archived (completed) courses from the toolbar.</p> </li> </ul>"},{"location":"#getting-started","title":"Getting started","text":"<p>Go to \"Packages\" on the main page https://github.com/MikloCO/ProjectTracker and download the bin for you operating system. </p>"},{"location":"#adding-your-first-course","title":"Adding your first course","text":"<ol> <li>Click New \u2192 Add new course in the menu bar.</li> <li> <p>Fill in the course details:</p> Field Description Title Name of the course Provider Platform (e.g. Udemy, YouTube) Link Optional URL to the course Number of chapters How many chapters to create Banner image Cover image shown on the card Category <code>art</code>, <code>programming</code>, <code>ai</code>, or a custom label In progress Check to add directly to the Overview </li> <li> <p>Click OK \u2014 the course card appears immediately.</p> </li> </ol>"},{"location":"#tracking-progress","title":"Tracking progress","text":""},{"location":"#completing-chapters","title":"Completing chapters","text":"<p>Open a course by clicking its card in Overview or Browse. The left panel lists all chapters as checkboxes.</p> <p>Tip</p> <p>Checking a chapter updates the progress bar on the course card in real time \u2014 no need to restart or switch tabs.</p>"},{"location":"#course-status-flow","title":"Course status flow","text":"<pre><code>todo  \u2500\u2500\u25ba  in_progress  \u2500\u2500\u25ba  completed\nBrowse        Overview          Archived\n</code></pre> <p>Change a course's status from within the course detail view to move it between sections.</p>"},{"location":"#workbook","title":"Workbook","text":"<p>The right panel of every course detail view is a full rich-text editor.</p> Action Toolbar button Bold B Italic I Underline U\u0332 Bullet list Numbered list Undo / Redo  /  Clear formatting <p>Auto-save</p> <p>Notes are saved automatically as you type. Each course stores its workbook in <code>~/.course_tracker/workbooks/course_&lt;id&gt;.html</code>.</p>"},{"location":"#gallery","title":"Gallery","text":"<p>Click the + button in the middle panel to pick one or more images from your filesystem. Images are copied into <code>~/.course_tracker/images/course_&lt;id&gt;/</code> so the originals can be moved or deleted safely.</p>"},{"location":"#keyboard-navigation","title":"Keyboard navigation","text":"Action How Switch to Overview Toolbar \u2192 Overview Switch to Browse Toolbar \u2192 Browse Switch to Archived Toolbar \u2192 Archived Add a course Menu \u2192 New \u2192 Add new course Open a course Click the course card"},{"location":"#data-storage","title":"Data storage","text":"<p>All course data is persisted to <code>data/courses.json</code> in the project directory. Workbook notes and gallery images live in <code>~/.course_tracker/</code>.</p> <p>Warning</p> <p>Do not manually edit <code>courses.json</code> while the application is running \u2014 changes will be overwritten on the next save.</p>"},{"location":"api/","title":"API","text":""},{"location":"api/#app.domain.manager.CourseManager","title":"<code>CourseManager</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Central manager responsible for handling course data, updates, and persistence.</p> <p>This class acts as the single source of truth for all course-related operations, including creation, modification, and saving to disk.</p> <p>Attributes:</p> Name Type Description <code>courses</code> <code>list[Course]</code> <p>List of all loaded courses.</p> <code>max_active</code> <code>int</code> <p>Maximum number of courses allowed to be active simultaneously.</p> <code>current_course</code> <code>Course | None</code> <p>Currently selected course.</p> Source code in <code>app\\domain\\manager.py</code> <pre><code>class CourseManager(QObject):\n    \"\"\"\n    Central manager responsible for handling course data, updates, and persistence.\n\n    This class acts as the single source of truth for all course-related operations,\n    including creation, modification, and saving to disk.\n\n    Attributes:\n        courses (list[Course]): List of all loaded courses.\n        max_active (int): Maximum number of courses allowed to be active simultaneously.\n        current_course (Course | None): Currently selected course.\n    \"\"\"\n\n    course_updated = Signal(int)  # emitted with course_id when a course changes\n\n    def __init__(self, courses: list[Course], max_active: int = 3):\n        \"\"\"\n        Initialize the CourseManager.\n\n        Args:\n            courses: Preloaded list of Course objects.\n            max_active: Maximum number of active courses allowed.\n        \"\"\"\n        super().__init__()\n        self.courses = courses\n        self.max_active = max_active\n        self.current_course = None\n\n    def add_course(self, course: Course):\n        \"\"\"\n        Add a new course to the manager.\n\n        Args:\n            course: The Course instance to add.\n        \"\"\"\n        self.courses.append(course)\n\n    def create_and_save_course(\n        self,\n        title: str,\n        provider: str,\n        link: str,\n        num_chapters: int,\n        banner_path: str | None = None,\n        category: str | None = None,\n        status: str = \"todo\",\n        project_path: str | None = None,\n    ):\n        \"\"\"\n        Create a new course with generated chapters and persist it.\n\n        Args:\n            title: Name of the course.\n            provider: Platform or provider of the course.\n            link: Optional URL to the course.\n            num_chapters: Number of chapters to generate.\n            banner_path: Optional path to a banner image.\n            category: Optional category label.\n            status: Initial course status (default is \"todo\").\n        \"\"\"\n        new_id = max([c.id for c in self.courses], default=0) + 1\n\n        chapters = [\n            Chapter(id=i + 1, title=f\"Chapter {i + 1}\") for i in range(num_chapters)\n        ]\n\n        new_course = Course(\n            id=new_id,\n            title=title,\n            provider=provider,\n            link=link if link else None,\n            banner_path=banner_path,\n            category=category,\n            project_path=project_path,\n            chapters=chapters,\n            status=status,\n        )\n\n        self.add_course(new_course)\n        save_courses(self.courses)\n\n    def update_status(self, course_id: int, status: str):\n        \"\"\"\n        Update the status of a course and persist the change.\n\n        Args:\n            course_id: ID of the course to update.\n            status: New status value.\n        \"\"\"\n        course = self.get(course_id)\n        course.status = status\n        save_courses(self.courses)\n\n    def get(self, course_id: int) -&gt; Course:\n        \"\"\"\n        Retrieve a course by its ID.\n\n        Args:\n            course_id: ID of the course.\n\n        Returns:\n            The matching Course instance.\n\n        Raises:\n            StopIteration: If no course with the given ID exists.\n        \"\"\"\n        return next(c for c in self.courses if c.id == course_id)\n\n    def update_course(\n        self,\n        course_id: int,\n        title: str,\n        provider: str,\n        link: str,\n        num_chapters: int,\n        banner_path: str | None,\n        category: str | None,\n        status: str,\n        project_path: str | None = None,\n    ):\n        \"\"\"Update an existing course's metadata, adding new chapters if num_chapters increased.\"\"\"\n        course = self.get(course_id)\n        course.title = title\n        course.provider = provider\n        course.link = link if link else None\n        course.banner_path = banner_path if banner_path else None\n        course.category = category\n        course.project_path = project_path\n        course.status = status\n\n        current_count = len(course.chapters)\n        if num_chapters &gt; current_count:\n            for i in range(current_count, num_chapters):\n                course.chapters.append(Chapter(id=i + 1, title=f\"Chapter {i + 1}\"))\n\n        save_courses(self.courses)\n        self.course_updated.emit(course_id)\n\n    def set_chapter_completed(self, course_id: int, chapter_id: int, completed: bool):\n        \"\"\"\n        Mark a chapter as completed or not and persist the change.\n\n        Args:\n            course_id: ID of the course.\n            chapter_id: ID of the chapter within the course.\n            completed: Whether the chapter is completed.\n        \"\"\"\n        course = self.get(course_id)\n        chapter = next(c for c in course.chapters if c.id == chapter_id)\n        chapter.completed = completed\n        save_courses(self.courses)\n        self.course_updated.emit(course_id)\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.__init__","title":"<code>__init__(courses, max_active=3)</code>","text":"<p>Initialize the CourseManager.</p> <p>Parameters:</p> Name Type Description Default <code>courses</code> <code>list[Course]</code> <p>Preloaded list of Course objects.</p> required <code>max_active</code> <code>int</code> <p>Maximum number of active courses allowed.</p> <code>3</code> Source code in <code>app\\domain\\manager.py</code> <pre><code>def __init__(self, courses: list[Course], max_active: int = 3):\n    \"\"\"\n    Initialize the CourseManager.\n\n    Args:\n        courses: Preloaded list of Course objects.\n        max_active: Maximum number of active courses allowed.\n    \"\"\"\n    super().__init__()\n    self.courses = courses\n    self.max_active = max_active\n    self.current_course = None\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.add_course","title":"<code>add_course(course)</code>","text":"<p>Add a new course to the manager.</p> <p>Parameters:</p> Name Type Description Default <code>course</code> <code>Course</code> <p>The Course instance to add.</p> required Source code in <code>app\\domain\\manager.py</code> <pre><code>def add_course(self, course: Course):\n    \"\"\"\n    Add a new course to the manager.\n\n    Args:\n        course: The Course instance to add.\n    \"\"\"\n    self.courses.append(course)\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.create_and_save_course","title":"<code>create_and_save_course(title, provider, link, num_chapters, banner_path=None, category=None, status='todo', project_path=None)</code>","text":"<p>Create a new course with generated chapters and persist it.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Name of the course.</p> required <code>provider</code> <code>str</code> <p>Platform or provider of the course.</p> required <code>link</code> <code>str</code> <p>Optional URL to the course.</p> required <code>num_chapters</code> <code>int</code> <p>Number of chapters to generate.</p> required <code>banner_path</code> <code>str | None</code> <p>Optional path to a banner image.</p> <code>None</code> <code>category</code> <code>str | None</code> <p>Optional category label.</p> <code>None</code> <code>status</code> <code>str</code> <p>Initial course status (default is \"todo\").</p> <code>'todo'</code> Source code in <code>app\\domain\\manager.py</code> <pre><code>def create_and_save_course(\n    self,\n    title: str,\n    provider: str,\n    link: str,\n    num_chapters: int,\n    banner_path: str | None = None,\n    category: str | None = None,\n    status: str = \"todo\",\n    project_path: str | None = None,\n):\n    \"\"\"\n    Create a new course with generated chapters and persist it.\n\n    Args:\n        title: Name of the course.\n        provider: Platform or provider of the course.\n        link: Optional URL to the course.\n        num_chapters: Number of chapters to generate.\n        banner_path: Optional path to a banner image.\n        category: Optional category label.\n        status: Initial course status (default is \"todo\").\n    \"\"\"\n    new_id = max([c.id for c in self.courses], default=0) + 1\n\n    chapters = [\n        Chapter(id=i + 1, title=f\"Chapter {i + 1}\") for i in range(num_chapters)\n    ]\n\n    new_course = Course(\n        id=new_id,\n        title=title,\n        provider=provider,\n        link=link if link else None,\n        banner_path=banner_path,\n        category=category,\n        project_path=project_path,\n        chapters=chapters,\n        status=status,\n    )\n\n    self.add_course(new_course)\n    save_courses(self.courses)\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.get","title":"<code>get(course_id)</code>","text":"<p>Retrieve a course by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>course_id</code> <code>int</code> <p>ID of the course.</p> required <p>Returns:</p> Type Description <code>Course</code> <p>The matching Course instance.</p> <p>Raises:</p> Type Description <code>StopIteration</code> <p>If no course with the given ID exists.</p> Source code in <code>app\\domain\\manager.py</code> <pre><code>def get(self, course_id: int) -&gt; Course:\n    \"\"\"\n    Retrieve a course by its ID.\n\n    Args:\n        course_id: ID of the course.\n\n    Returns:\n        The matching Course instance.\n\n    Raises:\n        StopIteration: If no course with the given ID exists.\n    \"\"\"\n    return next(c for c in self.courses if c.id == course_id)\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.set_chapter_completed","title":"<code>set_chapter_completed(course_id, chapter_id, completed)</code>","text":"<p>Mark a chapter as completed or not and persist the change.</p> <p>Parameters:</p> Name Type Description Default <code>course_id</code> <code>int</code> <p>ID of the course.</p> required <code>chapter_id</code> <code>int</code> <p>ID of the chapter within the course.</p> required <code>completed</code> <code>bool</code> <p>Whether the chapter is completed.</p> required Source code in <code>app\\domain\\manager.py</code> <pre><code>def set_chapter_completed(self, course_id: int, chapter_id: int, completed: bool):\n    \"\"\"\n    Mark a chapter as completed or not and persist the change.\n\n    Args:\n        course_id: ID of the course.\n        chapter_id: ID of the chapter within the course.\n        completed: Whether the chapter is completed.\n    \"\"\"\n    course = self.get(course_id)\n    chapter = next(c for c in course.chapters if c.id == chapter_id)\n    chapter.completed = completed\n    save_courses(self.courses)\n    self.course_updated.emit(course_id)\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.update_course","title":"<code>update_course(course_id, title, provider, link, num_chapters, banner_path, category, status, project_path=None)</code>","text":"<p>Update an existing course's metadata, adding new chapters if num_chapters increased.</p> Source code in <code>app\\domain\\manager.py</code> <pre><code>def update_course(\n    self,\n    course_id: int,\n    title: str,\n    provider: str,\n    link: str,\n    num_chapters: int,\n    banner_path: str | None,\n    category: str | None,\n    status: str,\n    project_path: str | None = None,\n):\n    \"\"\"Update an existing course's metadata, adding new chapters if num_chapters increased.\"\"\"\n    course = self.get(course_id)\n    course.title = title\n    course.provider = provider\n    course.link = link if link else None\n    course.banner_path = banner_path if banner_path else None\n    course.category = category\n    course.project_path = project_path\n    course.status = status\n\n    current_count = len(course.chapters)\n    if num_chapters &gt; current_count:\n        for i in range(current_count, num_chapters):\n            course.chapters.append(Chapter(id=i + 1, title=f\"Chapter {i + 1}\"))\n\n    save_courses(self.courses)\n    self.course_updated.emit(course_id)\n</code></pre>"},{"location":"api/#app.domain.manager.CourseManager.update_status","title":"<code>update_status(course_id, status)</code>","text":"<p>Update the status of a course and persist the change.</p> <p>Parameters:</p> Name Type Description Default <code>course_id</code> <code>int</code> <p>ID of the course to update.</p> required <code>status</code> <code>str</code> <p>New status value.</p> required Source code in <code>app\\domain\\manager.py</code> <pre><code>def update_status(self, course_id: int, status: str):\n    \"\"\"\n    Update the status of a course and persist the change.\n\n    Args:\n        course_id: ID of the course to update.\n        status: New status value.\n    \"\"\"\n    course = self.get(course_id)\n    course.status = status\n    save_courses(self.courses)\n</code></pre>"}]}